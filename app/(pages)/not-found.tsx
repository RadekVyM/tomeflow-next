import NotFoundPageLayout from "@/app/components/error/NotFoundPageLayout";
import ScrollablePageLayout from "../components/layout/ScrollablePageLayout";

export default function NotFoundPage() {
    return (
        <ScrollablePageLayout>
            <NotFoundPageLayout />
        </ScrollablePageLayout>
    );
}